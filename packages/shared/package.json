{
  "name": "@dineflow/shared",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "node -e \"process.exit(require('child_process').spawnSync('tsc', ['-b'], { shell: true, stdio: 'inherit' }).status)\""
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
